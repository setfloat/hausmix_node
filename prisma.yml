# endpoint: ${env:PRISMA_ENDPOINT}
# endpoint: https://hausmix-production-306b2d1567.herokuapp.com/hausmix-prod/prod
endpoint: http://api.hausmix.com
datamodel: datamodel.prisma
# secret: ${env:PRISMA_SECRET}
hooks:
  post-deploy:
    - graphql get-schema -p prisma
